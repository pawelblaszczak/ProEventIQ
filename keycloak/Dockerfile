# Example Dockerfile that bakes the custom theme into a Keycloak image
FROM quay.io/keycloak/keycloak:26.3.2

# Copy themes into the Keycloak themes directory
COPY themes /opt/keycloak/themes

# Entrypoint and cmd are inherited from upstream image
